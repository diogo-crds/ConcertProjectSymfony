{% extends 'base.html.twig' %}

{% block title %}Accueil{% endblock %}

{% block body %}
  <style>
      .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
      .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
  </style>

  <div class="example-wrapper">
      <h1>Projet Symfony Concert 🎤🎸🔉</h1>
      <br>
      <h4>Quels sont les prochains concerts à venir ?</h4>

  <table class="table table-striped">
    <thead>
      <tr>
        <th scope="col">Tournée</th>
        <th scope="col">Date</th>
      </tr>
    </thead>

    {% for concert in concerts %}
      {% if(concert.date|date('Y-m-d') > "now"|date('Y-m-d')) %}
      <td>{{ concert.tourName }}</td>
      <td>{{ concert.date|format_datetime(locale='fr',pattern="dd MMMM YYYY")|title }}</td>
      {% endif %}
    {% endfor %}
  </div>
  {% endblock %}
